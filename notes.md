# components

## falls man passthrough components baut

provide + inject, wenn componenten verschachtelt sind und nicht direkt zueinander stehen. Voraussezung sie stehen in einer kette zueinander

sonst immer props und emit

funktioniert auch mit functions
